export default async function readClubFeed({ clubId }: { clubId: string }) {
  // const res = await fetch(`${process.env.SERVER_URL}/club/{id}/feed`, {
  //   method: 'GET',
  //   // cache: 'no-store',
  // });
  // if (!res.ok) {
  //   throw new Error('동아리 소개를 가져오는 데 실패했습니다.');
  // }
  // return res.json() as Promise<IClubFeed[]>;
}
